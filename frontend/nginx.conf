server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    # Static SPA only. /api/*, /healthz and /readyz are routed to the backend by
    # an upstream reverse proxy, not by this container.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
